import { Layout } from './common/Layout';
import {
	TopBarBtns,
	WelcomeMsg,
	SearchBar,
	CategoryList,
} from './screens/dashboard';

const Components = {
	Layout,
	TopBarBtns,
	WelcomeMsg,
	SearchBar,
	CategoryList,
};

export default Components;
