import {
  SafeAreaView,
  View,
  Text,
  TouchableOpacity,
  Image,
  ScrollView,
  FlatList,
  TextInput,
  StyleSheet,
} from "react-native";
import { CustomBtn, CustomText, CustomView } from "../../custom";

const RN = {
  SafeAreaView,
  View,
  Text,
  TouchableOpacity,
  Image,
  ScrollView,
  FlatList,
  TextInput,
  StyleSheet,
  CustomView,
  CustomText,
  CustomBtn,
};

export default RN;
