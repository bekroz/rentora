import {
	View,
	Text,
	TouchableOpacity,
	Image,
	ScrollView,
	FlatList,
} from 'react-native';

const RN = {
	View: View,
	Text: Text,
	TouchableOpacity: TouchableOpacity,
	Image: Image,
	ScrollView: ScrollView,
	FlatList: FlatList,
};

export default RN;
