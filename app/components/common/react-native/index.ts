import {
	View,
	Text,
	TouchableOpacity,
	Image,
	ScrollView,
	FlatList,
	TextInput,
} from 'react-native';

const RN = {
	View: View,
	Text: Text,
	TouchableOpacity: TouchableOpacity,
	Image: Image,
	ScrollView: ScrollView,
	FlatList: FlatList,
	TextInput: TextInput,
};

export default RN;
